# Model Assets

- `simple-f1.gltf` is a lightweight static F1-style model generated for this local prototype. The app loads and clones it through `GLTFLoader`, then tints the team-paint material per driver.
